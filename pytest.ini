[pytest]
# В репозитории есть независимые test-пакеты приложения и публичного сайта.
# Importlib mode позволяет pytest собирать оба дерева без конфликта имён.
addopts = --import-mode=importlib
pythonpath = . site
